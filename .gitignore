dist/
*.exe
state/state.json
.claude/
.remember/
.superpowers/
/csm
.cache/
graphify-out/

# Internal planning / local-only files
CLAUDE.md
AGENTS.md
docs/IMPROVEMENT_PLAN.md
docs/abuse-reporting-implementation-plan.md
docs/superpowers/
scripts/deploy-gitlab.sh

# Local daemon artifacts produced if dev runs point state/cert paths at source dirs
internal/daemon/dircache.json
internal/daemon/fileindex.current
internal/daemon/fileindex.previous
internal/daemon/webui.crt
internal/daemon/webui.key

# Environment / secrets
.env
.env.local
.env*.local

# TLS certificates and keys
*.key
*.pem
*.crt

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Build artifacts
*.o
*.a
*.so

# bpf2go-generated BPF object files are embedded into the Go bindings via
# //go:embed and must be tracked. Re-include them despite the *.o rule above.
!internal/daemon/*_bpfprog/*.o

# mdBook output
docs/book/
