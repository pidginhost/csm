Synthetic MaxMind DB v2 fixtures with one IPv4 record for 192.0.2.0/24.
The ASN is private-use AS64512 (Example Network); the city is Example City, US.
Each tree has 24 nodes with 24-bit records, a 16-byte data separator,
a single map record, and standard MMDB metadata. No external data is used.
